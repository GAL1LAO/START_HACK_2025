before running the streamlitui.py please do
    pip install streamlit
    pip install pandas
    pip install altair
    pip install streamlit_shadcn_ui
then start the script with 
    python -m streamlit run <file_path>